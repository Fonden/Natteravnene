@model IList<AFile>
@for (int i = 0; i < Model.Count(); i++)
{
    <div class="col-lg-1">
        @Html.HiddenFor(m => m[i].FileID)
        @Html.TextBoxFor(m => m[i].Title)
    </div>
    <div class="col-lg-1">

    </div> 
        }
