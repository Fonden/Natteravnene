@model AFile
@section pagescripts {
    @Scripts.Render("~/bundles/form")
}
@section inlinescripts {
    <script type="text/javascript">


        jQuery(function ($) {
           


 


        

        });
    </script>
    }
<div class="page-header position-relative">
    <h1>
        <a href="@Url.Action("Index")" class="btn btn-small btn-primary">
            <i class="ace-icon @Icons.BackIcon icon-only"></i>
        </a> @(Model.FileID == Guid.Empty ? General.FileCreate : General.FileEdit)
    </h1>
</div>



    @*using (Ajax.BeginForm("_Upload", "File", new { area = "Admin" }, new AjaxOptions() { HttpMethod = "POST" }, new { enctype = "multipart/form-data", id = "image-upload-form", @class = "form-horizontal" }))*@
    @using (Html.BeginForm("Edit", null, FormMethod.Post, new { @class = "form-horizontal", enctype = "multipart/form-data"}))
    {

       

        <div class="clearfix"></div>
        <div class="space-4"></div>
  


    @Html.Partial("~/Views/Shared/_FormSummery.cshtml");
    @Html.HiddenFor(m => m.FileID)
    
    <div class="form-group">
        @Html.LabelFor(m => m.Title, new { @class = "col-sm-2 control-label no-padding-right" })
        <div class="col-sm-8">
            @Html.TextBoxFor(m => m.Title, new { @class = "form-control", placeholder = DomainStrings.Title })
            @Html.ValidationMessageFor(m => m.Title)
        </div>
    </div>

    <div class="space-4"></div>

    <div class="form-group">
        @Html.LabelFor(m => m.Description, new { @class = "col-sm-2 control-label no-padding-right" })
        <div class="col-sm-8">
            @Html.TextAreaFor(m => m.Description, new { @class = "form-control", placeholder = DomainStrings.Description })
            @Html.ValidationMessageFor(m => m.Description)
        </div>
    </div>

    
    <div class="space-4"></div>
    <div class="form-actions">
        <button class="btn btn-info" type="submit">
            <i class="icon-ok bigger-110"></i>
            @(Model.FileID == Guid.Empty ? General.Create : General.Save)
        </button>
        &nbsp; &nbsp; &nbsp;
        <button class="btn" type="reset">
            <i class="icon-undo bigger-110"></i>
            @General.Reset
        </button>
    </div>
    }
